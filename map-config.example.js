/**
 * Local Mapbox token (browser-safe public token, starts with pk.).
 *
 * Setup:
 * 1. Copy this file to `map-config.js` in the same folder as map.html.
 * 2. Replace YOUR_MAPBOX_PUBLIC_TOKEN below.
 * 3. `map-config.js` is gitignored so your token is not committed.
 *
 * Get a token: https://account.mapbox.com/access-tokens/
 */
window.MAPBOX_ACCESS_TOKEN = "YOUR_MAPBOX_PUBLIC_TOKEN";
